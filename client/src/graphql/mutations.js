import gql from 'graphql-tag';
/*
export const  CREATE_ACTORS= gql`
mutation createActors(
  	$data: ActorCreateInput!
) {
  createActor (
    data: $data
  ) {
    name
    id
  }
}
`

export const  DELETE_ACTORS= gql`
mutation deleteActors(
  $id: ID!
){
  deleteActor(where: {
    id: $id
  }) {
    id
    name
  }
}
`
*/